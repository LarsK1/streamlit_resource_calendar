import React from "react"
import ReactDOM from "react-dom"
import StreamlitTimeline from "./resourcecalendar"

ReactDOM.render(
  <React.StrictMode>
    <ResourceCalendar />
  </React.StrictMode>,
  document.getElementById("root")
)